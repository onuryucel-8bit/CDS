#ifndef CDS_GRAPH_LL_H
#define CDS_GRAPH_LL_H

typedef struct stdGraphLL graph;

struct stdGrapLL{

};

typedef struct stdGraphLL_adjList graph;

void CDS_GRAPHLL_init();

void CDS_GRAPHLL_add();

#endif // CDS_GRAPH_H
