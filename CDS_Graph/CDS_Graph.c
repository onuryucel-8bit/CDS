#include<CDS_Graph.h>

void CDS_graph_lil_init(){

    cdst_array* adjaceny_array = CDS_dynamicArray_init(5);

}

void CDS_graph_lil_add(){
    cdst_linkedList_List* node_list = CDS_LinkedList_init();

    CDS_dynamicArray_addLast(node_list);

    //CDS_LinkedList_addLast(graph.array,int data);
}
